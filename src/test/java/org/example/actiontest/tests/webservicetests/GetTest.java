package org.example.actiontest.tests.webservicetests;

import io.restassured.RestAssured;
//import org.delta.automation.automationcoreframework.logger.Log;
//import org.delta.automation.webservices.automationbaseclasses.BaseWebServiceTest;
//import org.testng.annotations.Test;
import static io.restassured.RestAssured.given;

/**
 * Web service test for the GET verb using Rest Assured.
 */
//public class GetTest extends BaseWebServiceTest {

//    private static int successStatusCode = 200;

    /**
     * Checks the status code for a GET request.
     */
/*    @Test
    public void getEmployeesStatusCodeTest() {
        Log.LOGGER.info("WebServiceURI = " + RestAssured.baseURI);
        Log.LOGGER.info("WebServicePort = " + RestAssured.port);
        Log.LOGGER.info("WebServicePath = " + RestAssured.basePath);
        given().
        when().
        get("GetEmployees").
        then().
        statusCode(successStatusCode);
    }
}
*/